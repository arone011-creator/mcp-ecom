================================================================================
IMPLEMENTATION PLAN - M4 / PHASE 2, STOREFRONT SHARE
The storefront's half of "one agent, with a chat UI". Written 29 Aug 2026.
================================================================================

M4 is split across two repositories, because the work genuinely is.

    THIS PLAN          the storefront: token refresh, the chat UI, the event
                       stream to the browser, and the approval control
    THE OTHER PLAN     mcp-ecom-agent-layer/docs/PLAN_M4_AGENT.txt - the
                       agent itself, its evaluation harness, and its deploy

Neither is complete alone. Read both before starting either.

    IN PLAIN TERMS
    The next stage adds an AI assistant customers can talk to. Half that
    work is the chat window and the "are you sure?" button, which live in
    the shop. The other half is the AI itself, which lives in the other
    project. This document covers the shop's half.


WHAT THIS PLAN IS, AND IS NOT
-----------------------------
It is a TASK-LEVEL plan: what to build, in what order, which files, what
each task must prove before it is done, and which decisions must be made
first.

It is NOT a step-by-step script with the code pre-written. That is
deliberate, and it is this project's own hard lesson: the M3 plan was
written against the real code rather than against design documents, and
that is what caught three places where the design was wrong about its own
system. Writing the UI code now, against components that do not exist and a
streaming contract not yet frozen, would produce confident fiction.

Write each task's step-level plan against the real code, at the time.

    IN PLAIN TERMS
    This lists what to build and in what order, not the finished code.
    Writing the code in advance - before the parts it depends on exist -
    has already been proven on this project to produce plans that are
    wrong in ways nobody notices until late.


CONTENTS
--------
     1. Decisions that must be made first
     2. What this half delivers
     3. The event contract - freeze it before anything consumes it
     4. Task list
     5. Exit criteria
     6. Risks carried in


================================================================================
1. DECISIONS THAT MUST BE MADE FIRST
================================================================================

Four. Each changes the task breakdown, so settle them before Task 1.

(A) WHERE DOES THE AGENT LOOP RUN?

    The chat UI is Next.js. The agent is Python. Something must sit between
    them. Two options:

      1. The browser talks to a Next.js route, which talks to the agent
         service over HTTP. One public surface, session cookie already
         present, CORS is not a problem.

      2. The browser talks to the agent service directly. Fewer hops, but
         a second public service handling browser sessions, and the
         approval control would live outside the app that authenticates
         the user.

    RECOMMENDED: option 1. It keeps every browser-facing concern in the
    storefront, which is the thing that already knows who is signed in.
    It also keeps the approval mint reachable only from server-side code.

(B) WHO MINTS THE APPROVAL - AND IT MUST NOT BE THE AGENT.

    This is the load-bearing decision of the whole milestone.

    The MCP server refuses a high-risk call without an approval token, and
    mints tokens from an HTTP route that is deliberately not an AI tool. But
    "not a tool" only helps if the agent process cannot make arbitrary HTTP
    calls to that route.

    THE RULE: the approval is minted by the Next.js route handler, after a
    human clicks, and handed to the agent as the argument of the next tool
    call. The agent never holds the ability to mint one.

    If the agent process can reach /approvals, the entire mechanism is
    theatre. Write a test that asserts the agent's egress cannot.

(C) HOW DOES THE BROWSER GET A BEARER TOKEN?

    The MCP server needs one, and it cannot be the seven-day default -
    these tokens cannot be revoked. Phase 2 owes a refresh mechanism
    (decided during M3, recorded in docs/mcp/phase-2-single-agent.md).

    RECOMMENDED: the browser never sees a bearer token at all. The Next.js
    route holds the session cookie, mints a fifteen-minute token
    server-side per request, and passes it onward. Nothing long-lived
    exists in the browser, and "refresh" becomes "mint another one",
    which needs no new storage.

(D) WHERE DOES THE CONVERSATION LIVE, AND HOW DOES IT SURVIVE NAVIGATION?

    The assistant is available from every page, not one dedicated route -
    a floating entry point, not a destination. That means the conversation
    and its streaming connection cannot be owned by a page component; a
    route change would tear both down.

    RECOMMENDED: a single provider mounted once in app/layout.tsx, holding
    the message history and the open connection to /api/assistant. Every
    page renders the same floating widget reading from that provider.
    Closing the widget hides it; it does not unmount the conversation.

    IN PLAIN TERMS
    Four things to decide before building. The most important: the AI
    must not be able to approve its own actions. A human clicks the
    button, and the shop - not the AI - issues the permission slip. If the
    AI could issue its own, the whole safety design would be decoration.
    The fourth: the chat needs to live "above" any one page, the way the
    header does, so clicking around the site does not reset it.


================================================================================
2. WHAT THIS HALF DELIVERS
================================================================================

    NEW FILES (expected)

    app/api/v1/auth/refresh/route.ts     Mint a short-lived bearer from the
                                         signed-in session. No password.
    app/api/assistant/route.ts           The bridge: browser <-> agent
                                         service. Holds the session, mints
                                         tokens, streams events back.
    app/api/assistant/approve/route.ts   Mints an approval AFTER a human
                                         click. Server-side only.
    components/assistant/AssistantProvider.tsx
                                         Root-level state: the conversation
                                         and the streaming connection,
                                         mounted once in app/layout.tsx so
                                         both survive client-side
                                         navigation.
    components/assistant/AssistantWidget.tsx
                                         The floating entry point and
                                         docked panel - collapsed by
                                         default on every page, not a
                                         route of its own.
    components/assistant/*.tsx           Message list, tool-activity chips,
                                         approval card, product/order cards.
    lib/assistant/events.ts              The event schema and its parser -
                                         one definition, shared.

    MODIFIED

    app/api/v1/auth/token/route.ts       Reuse for the refresh path, or
                                         factor the minting into a helper
                                         both routes call.
    scripts/scorecard.ts                 An `agent` section: workflow pass
                                         rate, turn latency, tokens.

    IN PLAIN TERMS
    A chat window available from every page, the plumbing behind it, a
    way to hand the AI a short-lived pass, and an approval button that
    only a person can press.


================================================================================
3. THE EVENT CONTRACT - FREEZE IT BEFORE ANYTHING CONSUMES IT
================================================================================

The design document is explicit that this is the cheapest thing to get
wrong: the chat UI, the agent, and Phase 3's pause-and-resume payload all
depend on the same event shape.

    tool_started        which tool, and the arguments, STRUCTURED
    tool_completed      which tool, and a summary of the result
    approval_required   which tool, the arguments, and a token handle
    message             assistant text
    error               something failed, and whether a retry is sensible

FOUR RULES THAT ARE NOT NEGOTIABLE:

  1. THE APPROVAL CARD IS RENDERED FROM THE STRUCTURED ARGUMENTS, NEVER
     FROM AGENT PROSE. Product descriptions and reviews are written by
     strangers and reach the agent's context. If the agent writes the words
     next to the button, an injected review can write them too. The card
     shows the order number the API returned - not a sentence the model
     composed.

  2. VERSION THE SCHEMA FROM THE FIRST COMMIT. Phase 3 consumes it too.

  3. CHAT-DRIVEN CHANGES UPDATE THE SAME DATA THE REST OF THE SITE READS,
     NOT A PRIVATE COPY. A tool_completed event for a cart or order change
     invalidates the same cache/query the manually-clicked cart page and
     header badge already use. Two copies of "what's in your cart" is a
     worse bug than a stale one - it lies to the customer about which
     answer is real.

  4. THE ASSISTANT NEVER NAVIGATES, FILTERS, OR SIGNS THE CUSTOMER IN OR
     OUT ON ITS OWN. It can show a link to a product; it does not route
     the customer there itself. A page changing underneath a customer
     without them acting is worse than the assistant not doing it at all.

    IN PLAIN TERMS
    The chat window shows what the AI is doing as it happens. The rule
    that matters most: the confirmation box must be built from real data -
    the actual order number - and never from a sentence the AI wrote. A
    review written by a stranger could otherwise put misleading words next
    to the confirm button. The other two rules keep the assistant from
    ever lying about what is really in your cart, or taking you somewhere
    you did not ask to go.


================================================================================
4. TASK LIST
================================================================================

Each task ends in a commit, and stops for review. Every task that changes
behaviour must include a test that has been seen to FAIL before it passes -
this project has already shipped, and caught, tests that could not fail.

TASK 1 - FREEZE THE EVENT SCHEMA    [DONE 2026-09-03]
    Define the five event types and their fields in lib/assistant/events.ts,
    with a parser and a version field.
    MUST PROVE: an unknown event type is ignored rather than crashing the
    UI, and a malformed event does not take down the stream.

    ALREADY FROZEN, 2026-09-02, by the agent plan's Task 4. Do not design
    it again - implement it. The canonical definition is:

        mcp-ecom-agent-layer/contracts/README.md
        mcp-ecom-agent-layer/contracts/assistant-events.v1.json

    "The two must agree exactly" is enforced by an artefact rather than by
    two prose descriptions, because separate repositories in different
    languages have no module to share. That JSON file holds a recorded
    stream covering all five types beside the conversation replaying it
    must produce. The agent repository already executes it in Python.

    THIS TASK: vendor that file into the storefront and assert the
    TypeScript reducer reaches the identical `expected` conversation.
    A shape change on either side then reddens tests on that side, which
    is the point - keep the copy byte-identical rather than tidying it.

    TWO THINGS THE AGENT WILL NEVER SEND, contrary to section 3 above:

      * approval_required carries NO token handle, only call_id. Section
        3's summary line says "a token handle", but Task 5 below has the
        approve route mint AFTER a human click, from the arguments in the
        event. A handle minted by the agent would be the agent taking
        part in its own approval. call_id is enough: the event names
        WHICH order, and Task 5's fresh server-side lookup answers WHAT
        is true about it.

      * tool_completed carries NO cache-invalidation hint. Rule 3 still
        holds, but the tool -> cache-key mapping is written HERE. The
        event carries `tool` and `ok`; this side maps. An agent shipping
        cache keys would be asserting facts about a UI it cannot see.

    Also settled there and worth knowing before building the widget:
    every tool_started is guaranteed a tool_completed, including a call
    the agent refused to run, so a chip never spins forever; and a
    missing `seq` is reported as a gap rather than smoothed over,
    because a dropped event means the screen is not showing what
    happened.

    BUILT. lib/assistant/events.ts, with the golden stream vendored
    byte-for-byte at lib/assistant/assistant-events.v1.json. The
    TypeScript reducer reaches the identical `expected` conversation the
    Python one does; that assertion is the cross-repository anchor, and
    it was mutation-checked rather than assumed -- removing the
    duplicate-call guard makes it fail.

    THE DIVISION OF LABOUR, which Task 4 needs to know:

        parseEvent(raw)   -> AssistantEvent | null.  NEVER THROWS.
        replay(events)    -> Conversation.  Sees only parsed events.

    A null from parseEvent means DROP THIS FRAME AND KEEP GOING. It is
    not an error to surface and not a reason to stop reading the stream.

    ONE DELIBERATE ASYMMETRY WITH THE PYTHON, recorded so it does not
    read as drift later. agent/events.py::replay THROWS on an event from
    a future schema version -- right for a harness, which should stop
    loudly when handed something it does not understand. Wrong here: a
    throw mid-stream leaves the customer with half a conversation and no
    way forward. So this half drops the frame instead, and the module
    header says why.

    Fourteen malformed inputs are covered, including a future version, a
    fractional seq, an array, and a missing data object. 258 unit tests
    pass; type-check clean.

TASK 2 - SHORT-LIVED TOKENS FROM A SESSION, WITHOUT A PASSWORD    [DONE 2026-09-03]
    POST /api/v1/auth/refresh: read the session cookie, mint a 15-minute
    bearer. Reuse the ttlSeconds clamp added in M3.
    MUST PROVE: 401 without a session; the minted token is accepted by
    /api/v1/auth/whoami; the lifetime is 15 minutes, not 7 days; and the
    route never accepts a password.

    ALL FOUR PROVED. AND A FIFTH, WHICH THIS PLAN DID NOT ASK FOR AND
    WHICH DECIDES WHETHER THE OTHER FOUR MATTER:

        THE ROUTE MUST REFUSE A BEARER TOKEN.

    The obvious implementation calls requireApiUser, because every other
    v1 route does and its own header calls it "the single identity choke
    point". But requireApiUser accepts a bearer OR a cookie -- and a
    bearer is exactly what this route issues. Wired that way, the agent's
    own 15-minute token can call /refresh and receive another 15 minutes,
    indefinitely. The token stops expiring, and every reason the lifetime
    is short evaporates: these JWTs cannot be revoked, and rotating
    NEXTAUTH_SECRET is the only kill switch and signs out every browser.

    So this route calls getToken directly. Cookie only. A browser may
    refresh; a token may not. That reasoning is in the route's own header
    as well as here, because a future reader tidying it to match the
    other routes would silently create an un-expiring token.

    Mutation-checked rather than assumed: swapping getToken for
    requireApiUser makes the test fail, which is the point of having it.

    TWO SMALLER DECISIONS
      * The route NEVER READS A REQUEST BODY. Not "rejects a password" --
        never parses one, so there is nothing to bypass and nothing to
        regress. A body carrying email and password changes nothing.
      * The lifetime is not caller-controllable. clampTtl exists so a
        caller can ask for LESS than a long default; here there is no
        default worth shortening. A test asserts REFRESH_TTL_SECONDS sits
        inside the token route's own bounds, so the two cannot drift into
        disagreeing about a sane lifetime.

    The response omits `user`. The browser calling this already knows who
    it is, and a route that hands back identity invites a client to trust
    it instead of /whoami.

    A per-user rate limit bounds a runaway client: 20 mints per 5
    minutes, which a widget refreshing a 15-minute token never
    approaches. In-process, so per replica -- the same limitation the
    token route's limiter already documents.

TASK 3 - THE BRIDGE ROUTE
    POST /api/assistant: takes a user utterance, mints a token, calls the
    agent service, streams events back to the browser.
    MUST PROVE: it refuses an unauthenticated caller; it never puts the
    bearer token in a response body; a dropped connection does not leave
    the agent running with nowhere to send events.

TASK 4 - THE ASSISTANT WIDGET, READ-ONLY FIRST, MOUNTED GLOBALLY
    AssistantProvider in app/layout.tsx; AssistantWidget rendered on every
    page. Render the event stream: messages, and tool activity as it
    happens. No approvals yet.
    MUST PROVE: the three low-risk workflows are usable end to end -
    "what did I order recently", a product search, a stock check. The
    conversation survives a client-side navigation between at least two
    different page types (e.g. home -> a product page) without resetting
    or dropping the connection. And: assistant message text renders as
    plain text or a tightly restricted Markdown subset - no raw HTML, no
    auto-linkification of arbitrary domains, links honoured only to this
    storefront's own domain. Product descriptions already arrive wrapped
    as untrusted content by the MCP server (mcp-ecom-agent-layer,
    docs/superpowers/specs/2026-09-02-prompt-injection-design-pass.md);
    this task is what keeps a rendering bypass from turning that into a
    clickable phishing link.

TASK 5 - THE APPROVAL CONTROL
    The approval card, plus POST /api/assistant/approve.
    MUST PROVE, and these are the tests that matter most:
      * the card's text is built from structured arguments, and an agent
        message containing markup or instructions cannot alter it;
      * the card's facts (order number, items, total) come from a fresh
        server-side lookup of the order by id, not from whatever the
        approval_required event's payload claims - the event names WHICH
        order, the lookup answers WHAT is true about it, so a compromised
        or manipulated event payload cannot misrepresent what is being
        confirmed;
      * the approve route mints for the EXACT arguments in the event, not
        arguments supplied by the caller;
      * a second click does not mint a second approval;
      * declining sends nothing to the MCP server;
      * nothing anywhere shows the action as completed until the server
        confirms it happened - a High-risk action never renders
        optimistically, unlike a Low/Medium tool result, which may.

TASK 6 - RICH RESULTS AND PROGRESS
    Product cards and order cards instead of prose, per the design's UX
    list. Render from tool results, not from agent text - same reason as
    the approval card.

    Also from that list, and easy to forget because they are not features
    so much as manners:
      * SHOW THE PLAN for a multi-step request, updating as steps complete.
        Visibility into what it intends, without exposing raw reasoning.
      * PROGRESS INDICATORS for longer flows, INCLUDING FAILURE STATES with
        explicit recovery options - Retry, Choose another, Cancel. A flow
        that can only succeed visibly and fails silently is worse than one
        that never started.
    MUST PROVE: a failed tool call produces a visible failure with a way
    forward, not a stalled spinner. And: a cart or order change made
    through the assistant is visible on the actual cart/orders page
    without a manual refresh, because it went through the same data path
    a manual action would (Rule 3, section 3) - never a private copy the
    chat keeps to itself.

TASK 6b - ASK FOR MISSING INFORMATION AS A CHOICE, NOT A PROMPT
    When the agent needs something it does not have - which order, which
    size - offer selectable options rather than asking the customer to type
    an identifier. "Which order?" with cards, never "please provide your
    order ID".
    MUST PROVE: the options come from a tool result, so the customer can
    only choose something that actually exists and belongs to them.

TASK 7 - PERSISTENT CONTEXT
    "The second order" should resolve without the customer repeating an id,
    and the current context should be VISIBLE rather than silently inferred.
    MUST PROVE: what the UI displays as the active context is what the next
    tool call actually uses. A drift between them is the bug this task
    exists to prevent.

TASK 8 - SCORECARD
    Extend scripts/scorecard.ts with an `agent` section - workflow pass
    rate, turn latency, tokens per workflow - reading a results file the
    agent repo's eval harness writes, exactly as the `mcp` section reads
    metrics/mcp-latency.json today.
    MUST PROVE: a workflow pass rate below 1 fails the gate absolutely,
    with or without a previous entry. That mistake was already made once
    in M3 and caught by a test.

    THE FILE SHAPE IS SETTLED, 2026-09-03, by the agent plan's Task 7.
    Do not design it again. metrics/agent-evals.json:

        {
          "generatedAt": "2026-09-03T09:14:02Z",
          "model": "gpt-4.1",
          "runs": 5,
          "workflows": {
            "orders-recent": {
              "passRate": 1.0,
              "toolAccuracy": 1.0,
              "unexpectedToolCalls": 0,
              "p50": 10703,
              "p95": 12880,
              "promptTokens": 2637,
              "completionTokens": 155
            }
          }
        }

    loadAgent() is a copy of loadMcp(): read from disk, return null when
    the file is absent, so this harness still never needs a Python
    environment. It reads `.workflows`; the envelope exists because a
    pass rate means nothing without knowing which model produced it and
    over how many runs, and both belong next to the number in the report.

    GATE ABSOLUTELY on passRate < 1, the same call the mcp section
    already makes for successRate < 1: a workflow that works four times
    in five is broken, not slow.

    unexpectedToolCalls is worth surfacing even though it does not gate.
    It counts tools the agent called that its fixture neither required
    nor allowed - the signal that an injected instruction moved the agent
    even where the guarded tools themselves were never reached.

    p50/p95 are WHOLE-TURN wall clock, several model round trips and
    several MCP calls, so they are seconds rather than the milliseconds
    in the mcp section. Do not gate them against the mcp numbers; they
    measure different things.

    IN PLAIN TERMS
    Eight steps, each finishing with a review. The order is deliberate:
    agree the message format, then the security plumbing, then a chat
    window that can only read things, and only then the button that can
    change something. The riskiest piece is built last, on top of parts
    already proven.


================================================================================
5. EXIT CRITERIA
================================================================================

    * A signed-in customer can hold a conversation that uses the low-risk
      tools and get correct, own-account-only results.
    * A cancellation requires a visible confirmation, and the confirmation
      text is provably not written by the model.
    * The agent cannot mint its own approval - asserted by a test, not by
      an assumption.
    * No bearer token is ever exposed to the browser.
    * The assistant is reachable from every page as a persistent widget,
      not a single dedicated route, and a conversation survives
      client-side navigation.
    * A cart or order change made through the assistant is visible
      everywhere else on the site immediately, through the same data path
      a manual action would use - never a private copy.
    * `npm run scorecard -- m4-single-agent --gate` exits 0.
    * The four workflows in docs/mcp/phase-2-single-agent.md pass in the
      eval harness (owned by the agent repo's plan).

    IN PLAIN TERMS
    Done means: a customer can chat with it, it only ever sees their own
    data, cancelling something needs a real click, the AI cannot give
    itself permission, and the health check still passes.


================================================================================
6. RISKS CARRIED IN
================================================================================

    * PROMPT INJECTION HAS A DESIGN NOW, NOT YET CODE HERE. See
      mcp-ecom-agent-layer, docs/superpowers/specs/
      2026-09-02-prompt-injection-design-pass.md. The MCP server now marks
      untrusted free text at the source; Tasks 4 and 5 above specify this
      half's part (rendering restrictions, and a fresh server-side lookup
      backing the approval card) but neither is built yet.

    * SESSION TOKENS CANNOT BE REVOKED. Fifteen-minute lifetimes bound the
      exposure. Nothing removes it.

    * RATE LIMITING IS PER-INSTANCE AND IN MEMORY. An agent can issue many
      more calls per minute than a person. Re-check the budgets before this
      is exposed to real traffic.

    * THE MCP SERVER'S APPROVAL NONCES ARE PER-PROCESS. Single-use holds
      for one replica only.

    IN PLAIN TERMS
    Four known problems carried into this stage. The prompt-injection one
    now has a design behind it - untrusted text is marked at the source in
    the other repo - but the two pieces this repo owes (safe rendering,
    and an approval card that checks the real order rather than trusting
    the event) are still to build.

================================================================================

================================================================================
CHAT PERSISTENCE, PHASE 1 - CHRONOLOGICAL TRANSCRIPT    [DONE 2026-09-03]
================================================================================
    Not a numbered task in this plan. It came out of the persistence
    brainstorm (docs/superpowers/plans/2026-09-03-chat-persistence-roadmap.md)
    when the GPT-style layout requirement exposed a bug that was already
    shipped.

    THE BUG. The panel rendered every utterance, then every tool chip, then
    every assistant message - grouped by kind rather than by when. One
    exchange looks perfect, which is why no screenshot caught it. Two came
    out as Q1 Q2 A1 A2.

    The cause was in the contract, not the component: replay() returned
    three parallel lists (text, tools, errors) and so discarded the order
    events arrived in.

    WHAT CHANGED
      * replay() gained an ordered `timeline`, added the same additive way
        message_delta was: mirrored in agent/events.py and
        lib/assistant/events.ts, golden fixture re-vendored byte for byte.
        A tool is NAMED on the timeline, not embedded, because its state
        changes after it first appears and a snapshot would read "working"
        forever. One call is one item however many events it emits. A
        finished message replaces its fragments IN PLACE, so an answer
        keeps its position relative to tool calls that followed it.
      * The provider now groups events BY TURN rather than holding one flat
        array. Nothing else could say which reply answered which question.
        replay() runs once per turn and remains the only reducer.
      * The panel renders one block per turn: customer right, assistant
        left, in order.

    Mutation-tested on both sides: 4 mutations each, all caught, and the
    shared golden fixture catches most of them independently of the local
    tests - which is the entire reason that fixture exists.

    THIS GROUPING IS THE SHAPE PHASE 2 PERSISTS as ConversationTurn
    (utterance + events). It is not scaffolding.

    Phases 2-6 (storage and resume, history UI, titles, memory with a hard
    budget, summarisation) each get their own plan. The roadmap records the
    decisions so none of them is argued twice.

  2026-09-04 -- CHAT PHASE 2: STORAGE AND RESUME (plan
  docs/superpowers/plans/2026-09-03-chat-phase2-storage-and-resume.md)

    A conversation now survives closing the panel, a refresh and signing
    out. Reopening the assistant shows the chat you were last having.

    VERIFIED LIVE 2026-09-04, signed in as the seeded demo customer:
      * asked a question, got an answer;
      * FULL PAGE RELOAD -- the question, the tool chip and the answer all
        came back. This is the step that had never worked before;
      * asked a follow-up. GET /api/assistant/conversations/latest then
        reported ONE conversation (cmtmksgul0001k8vin2gfl90o) with two
        turns in order, 100 and 38 events -- it joined rather than forking,
        which is the part that depended on the x-conversation-id header
        being adopted;
      * signed out, signed back in: both turns still there;
      * navigated to /products with the site's own link: the panel kept
        the whole conversation. That was the case originally reported as
        broken.
    NOT verified: a genuine browser restart, which cannot be simulated from
    a driven session. Signing out drops the session entirely and is the
    stronger test, so this is a gap in evidence rather than in behaviour.

    WHAT CHANGED
      * Two tables, ONE migration: conversations and conversation_turns.
        The turn is the unit -- utterance plus the events that answered it
        -- because that is already the shape the provider holds, so
        persistence is append-only and hydration is one query.
        The migration also creates title, summary, summarisedThrough and
        agentContext, which nothing reads yet. Deliberate: schema changes
        against a live database are the riskiest step in this roadmap, the
        data model was settled in full during brainstorming, and four
        nullable columns now cost less than three more migrations later.
      * lib/assistant/conversation-store.ts is the ONLY place that touches
        these tables, and every query in it filters by userId inside the
        query rather than checking after it. That is structural, not
        stylistic: it is the M1 cached-order-read mistake.
      * The bridge accumulates the events it forwards and writes one row
        when the stream ends, awaited while the response is still open.
        Stored and forwarded are asserted to be the same events -- a record
        that disagrees with what was on screen is a second story about what
        happened. Control frames are withheld from the record exactly as
        they are from the browser.
      * The conversation is resolved BEFORE the bearer is minted or the
        agent is called, so someone else's conversation id costs a 404 and
        not a model call. It is created lazily on the first message, so
        opening the panel and changing your mind leaves nothing behind.
      * GET /api/assistant/conversations/latest takes no parameters at all.
        null is a normal answer, not a 404: everybody has a first visit.
      * The provider hydrates on mount and carries the conversation id,
        adopting it from the x-conversation-id response header. Stored
        events go through parseEvent -- the same door as live ones.

    Mutation-tested: 12 mutations across the store, the bridge and the
    provider, all caught.

    HOW THE MIGRATION REACHED PRODUCTION -- read this before the next one.
    prisma migrate deploy had NEVER run automatically on this project. The
    web service had no pre-deploy and no custom start command; the
    db:migrate:deploy script existed and a test asserted it existed, but
    nothing invoked it, so the schema was presumably applied by hand at
    some point. Option A was chosen: the web service now has a Railway
    pre-deploy command, `npm run db:migrate:deploy`. Every future deploy
    applies pending migrations before the new container takes traffic, and
    a failed migration blocks the deploy -- which is the correct failure.
    Deploy a2704c84 (commit fadd93a, 2026-09-04 06:26 UTC) reported
    "5 migrations found", applied 20260903190000_add_assistant_conversations
    and nothing else, and reached SUCCESS. So _prisma_migrations WAS
    already in step with the four earlier migrations -- the drift this
    change was braced for did not exist. Worth knowing next time.

    KNOWN GAP, ACCEPTED. A browser that disconnects mid-turn hits the
    stream's cancel(), not done, so that turn is not stored. Persisting
    there cannot be awaited and would be best-effort at the exact moment
    the runtime is tearing the handler down. The customer closed the tab
    and the agent's turn was cancelled with it.

    Phases 3-6 (history list and + button, titles, agent memory,
    summarisation) each still get their own plan.

  2026-09-04 -- CHAT PHASE 3: HISTORY UI (plan
  docs/superpowers/plans/2026-09-04-chat-phase3-history-ui.md)

    The panel has a + and a history button. The history lists every past
    chat by name and age, opens any of them, and deletes one behind a
    second deliberate click.

    WHAT CHANGED
      * lib/assistant/relative-time.ts -- "just now", "5m ago", "14 Aug".
        `now` is a parameter, so the tests are about the function rather
        than about a mocked clock, and dates are built by hand rather than
        through toLocaleDateString so CI does not disagree with a laptop.
      * Three more queries in conversation-store: listConversations,
        loadConversation, deleteConversation. The list fetches ONE turn per
        chat, not all of them. Delete uses deleteMany rather than delete,
        because delete throws when nothing matches and a thrown error is
        observably different from "not yours".
      * GET /api/assistant/conversations, and GET + DELETE on
        /conversations/[id]. Both handlers on [id] answer the SAME 404 for
        a chat that is not yours and one that does not exist.
      * The provider gained newChat, openConversation, deleteConversation
        and the list. newChat clears local state and STORES NOTHING.
      * components/assistant/conversation-list.tsx -- presentational, owning
        one piece of state: which row is armed for deletion.

    A BUG IN PHASE 2's CODE, FOUND BY THIS PHASE'S TESTS. The resume-on-
    mount handler called setTurns unconditionally, so a response landing
    AFTER the customer had already sent their first message deleted the
    turn they were watching and filed their next message into the resumed
    chat rather than the new one. Both setters are now functional and
    yield to whatever is already there. It would have surfaced as an
    intermittent "my message vanished", which is the kind that never gets
    reproduced.

    ONE MUTATION SURVIVED AND THE CODE WAS WRONG, NOT THE TEST. A
    Math.max(0, ...) clamp in relativeTime, written to stop a future
    timestamp reading as "-1m ago", could never change an answer: a
    negative age is below every threshold and the first branch claims it
    anyway. Removed rather than left as decoration that looks like a
    guard.

    Mutation-tested: 28 across the six tasks, 27 caught and the survivor
    dealt with above. One further attempt was discarded rather than
    counted -- it broke compilation, so the suite did not run and the
    result said nothing about the tests.

    VERIFIED LIVE 2026-09-04 on deploy 69cbce15 (commit 0e72d98), signed
    in as the seeded demo customer:
      * the header carries Start a new chat / Show chat history / Close /
        Send, all as icons with accessible names;
      * the history lists a chat named from its first message with a
        relative age, and HIDES the message box while it shows;
      * pressing + issued ZERO network requests and left the list
        unchanged -- instrumented by wrapping window.fetch, not eyeballed;
      * both header buttons were disabled mid-stream and enabled again
        after;
      * a new chat appeared in the list, named from the utterance, newest
        first;
      * opening an older chat restored its turns and left the other one
        behind;
      * the FIRST delete click armed and deleted nothing; Cancel backed
        out with both chats intact; the second click removed it;
      * deleting the chat that was open cleared the panel to its empty
        state rather than leaving a transcript with no rows behind it.

    THE FIRST DEPLOY OF THIS COMMIT FAILED FOR AN INFRASTRUCTURE REASON:
    "failed to solve: failed to load cache key: unable to lease content:
    lease does not exist". That is Railway's buildkit losing a cache layer,
    not the code -- next build compiled clean locally. A redeploy of the
    same commit succeeded. Worth recognising rather than debugging next
    time it appears.

    Phases 4-6 (titles, agent memory, summarisation) each still get their
    own plan. Phase 5 is the one that changes how the assistant feels: the
    chat is saved, but the agent has not read it.

  2026-09-04 -- CHAT PHASE 5: AGENT MEMORY (plan
  docs/superpowers/plans/2026-09-04-chat-phase5-agent-memory.md)

    The assistant reads the chat it is in. Reopen a conversation, ask
    "and the second one?", and it answers.

    HOW IT TRAVELS. The agent stays stateless. The bridge reads the
    earlier turns' agentContext, trims them to a token budget, and sends
    them as `history` in the request body. The agent's own record of the
    turn comes back on a SECOND control frame -- the channel that already
    carries the MCP session id and that the bridge already drops. So the
    model transcript reaches the database without a browser anywhere on
    the path, and no new leak path was opened to do it.

    WHAT CHANGED
      * lib/assistant/history-budget.ts -- estimateTokens and
        buildHistory. Turns are SELECTED newest-first and REPLAYED
        oldest-first. The walk stops at the first turn it cannot replay
        rather than skipping it, so history is always contiguous: a gap
        in the middle is worse than a shorter history, because the model
        cannot tell that anything was removed and reads what it can see
        as consecutive.
      * conversation-store gained loadAgentContext -- selects
        agentContext and nothing else, because it runs on every turn of
        every continuing conversation.
      * The bridge sends `history`, captures the context frame, and
        writes it to agentContext. It reads NOTHING for a new chat.

    THE BUDGET IS AN ESTIMATE, AND SAYS SO. Four characters of JSON per
    token, default 6000, overridable with ASSISTANT_HISTORY_TOKEN_BUDGET.
    A real tokeniser would be a new dependency to make a number more
    precise that only needs to be safe, and the number is set far below
    anything the model would refuse. The ceiling is about what a long
    conversation COSTS on every message, not about what fits.

    A TURN IS THE UNIT, AND THAT IS NOT TIDINESS. Each stored context
    opens with the customer's message, answers every tool call it makes
    and ends with prose, so any run of consecutive ones is a valid
    request. Half of one is a tool call nothing answered, which the API
    rejects outright -- so a turn that DIED hands back no context at all
    and is stored as null. Storing a half turn would break every LATER
    turn of that conversation, not just the one that failed.

    AN ALLOWLIST OF ROLES, NOT A BAN ON `system`. The roadmap says
    "refuse any system role". Written literally that is a rule about one
    string, and the API already has `developer` with the same authority.
    Three roles a turn can produce, three accepted, in both languages.
    The storefront DROPS a bad row (and everything older); the agent
    REFUSES the request with a 400. Two layers on purpose: dropping means
    a tampered row costs memory rather than the conversation, and the
    agent's refusal is the guarantee that does not depend on the
    storefront being right.

    PRE-PHASE-5 ROWS ARE HARMLESS BY THE SAME RULE. Every turn stored
    since Phase 2 holds agentContext: null. They are not replayable, the
    walk stops at them, and replay simply starts after the newest one. No
    migration, no backfill.

    A FAILED HISTORY READ IS LOGGED AND SKIPPED, NOT RAISED. A chat that
    cannot remember still answers. Failing the turn would take a working
    conversation down over a degraded feature the customer cannot act on.

    Mutation-tested: 12 across both repos, all 12 caught -- the role
    allowlists, the seed export, the contiguity rule, the replay order,
    the control-vs-assistant channel, sending history on a new chat, and
    storing the context.

    ONE TYPE ERROR THE TESTS COULD NOT HAVE FOUND. buildHistory indexed
    backwards through the array, which noUncheckedIndexedAccess correctly
    flags as possibly undefined. Rewritten to iterate a reversed copy
    rather than silenced with a non-null assertion: the copy costs
    nothing next to stringifying each turn, and the loop now reads as
    being about turns rather than about arithmetic.

    STILL OPEN AFTER THIS PHASE
      * An over-budget conversation loses its oldest turns ABRUPTLY, with
        nothing in their place. Phase 6 (summarisation) is what makes
        that graceful. The budget was built first deliberately, so the
        summariser improves a working floor rather than being the only
        thing holding the ceiling.
      * No cap on the size of a stored agentContext row. An enormous tool
        result is written in full and then never replayed, because it
        does not fit. Capping at write time would mean silently storing
        null for a turn that succeeded. If row size becomes a problem it
        will show up as a database measurement, not as a guess made now.
      * Memory is scoped to ONE conversation, deliberately. Two chats
        about two different orders must not contaminate each other.

    VERIFIED LIVE 2026-09-04 on web deploy bde4422 against agent deploy
    c14e75d (both confirmed by commit hash, and the agent additionally by
    the sha its /health reports -- a 200 proves A container is up, not
    that it is THIS one). Signed in as the seeded demo customer, with
    window.fetch wrapped so every request body and the whole response
    stream could be READ rather than eyeballed:

      * MEMORY WORKS. Turn 1 "what did I order recently?" answered with a
        numbered list. Turn 2 "and the second one?" answered with the
        MacBook Air M2 -- item 2 of that list -- and went on to fetch
        that order's totals, which means it resolved an order id it could
        only have got from the replayed turn.
      * MEMORY SURVIVES A REOPEN. Reopened the same chat from the history
        list and asked "and what did the first one cost?" as a THIRD
        turn: it answered with the iPhone 15 Pro's totals, item 1 of the
        list from turn 1. The resume path replays the same rows the live
        path does.
      * A NEW CHAT KNOWS NOTHING. Pressed + and asked "and the second
        one?" cold: it asked which second one, and the stream never
        mentioned the MacBook. Memory is scoped to the conversation.
      * THE CONTEXT NEVER REACHED THE BROWSER. Every response stream
        contained ONLY `event: assistant` frames -- no `event: control`,
        no "context", no "role", no "tool_call_id". Read off the captured
        stream text, not inferred from the panel.
      * THE PRE-PHASE-5 CHAT STILL WORKS. Reopened the 7-hour-old chat
        from the Phase 3 verification, whose every turn holds
        agentContext: null, and asked a follow-up. It answered cleanly
        with ZERO error events and no memory -- replay stopped at the
        null rows exactly as intended. No migration and no backfill were
        needed to make that safe.

    NOT VERIFIED LIVE: the budget actually dropping an oldest turn. Doing
    that honestly needs a conversation long enough to exceed 6000
    estimated tokens, which is many paid turns; it is covered by unit
    tests including a hundred-turn case, and by a bridge test that sets
    the budget to 40 and watches the older turn fall off the request.

  2026-09-04 -- CHAT PHASE 4: TITLES (plan
  docs/superpowers/plans/2026-09-04-chat-phase4-titles.md)

    A chat names itself after its first exchange. The history list reads
    as a list of subjects rather than four rows of the same question.

    THE REQUEST CARRIES NO BODY, AND THAT IS THE DESIGN. The obvious
    version has the browser POST the exchange it just watched. That would
    make the endpoint two things it must not be: a way to put
    attacker-chosen text in front of the model on this project's account,
    and a way to write a near-arbitrary string into a row rendered on
    every future page load. The route reads turn zero back out of the
    database instead -- one query, and the whole class is gone.

    IDEMPOTENT IN ONE QUERY, NOT A READ THEN A WRITE. nameConversation is
    a single updateMany with `title: null` in the where clause, so "still
    unnamed" and "yours" are the same atomic condition. A request fired
    twice -- which the browser is allowed to do here -- matches zero rows
    the second time rather than racing a read.

    WHAT CHANGED
      * agent/titles.py and POST /title in the agent: one cheap model
        call over BOTH halves of the first exchange, because the subject
        is often not in the question ("and the second one?" names
        nothing). No tools, no MCP session, no customer bearer -- naming
        is a text task, and giving this endpoint tools would put a second,
        quieter path to the customer's orders beside the one the approval
        design guards.
      * clean_title: no URLs, no control characters, one line, capped at
        60 -- the same width the fallback truncates at. None on anything
        doubtful, because the fallback is the customer's own words and
        beats a mangled model answer.
      * conversation-store gained firstExchange and nameConversation.
      * POST /api/assistant/conversations/{id}/title.
      * The provider asks once, after turn one, and ignores the outcome.

    NOTHING IS EVER A 500. Every failure -- agent down, agent slow, agent
    answering nonsense -- is a 200 with a null title and an untouched row.
    A chat always already has a usable name, so a naming failure is an
    answer, not an error the browser has to handle.

    EXISTING CHATS ARE DELIBERATELY NOT BACKFILLED. A chat asks for a name
    after its FIRST turn, so the ones already in the account keep their
    fallbacks forever. Backfilling would spend model tokens on old rows to
    change a string that already reads acceptably.

    Mutation-tested: 7, all caught -- the URL strip, both halves of the
    updateMany where clause, the already-named short circuit, the
    non-string guard, and both provider guards (once only, and not on a
    turn that produced nothing).

    ONE PROCESS MISTAKE WORTH RECORDING. A `git checkout` run to revert a
    mutation reverted an UNCOMMITTED implementation along with it, so the
    next mutation ran against code where the feature was simply absent and
    "3 tests failed" meant nothing. Committing before mutating is not
    tidiness; without it the mutation result is unreadable.

    VERIFIED LIVE 2026-09-04 on web deploy 2d9fd38 against agent deploy
    6ca2050 (sha 6ca2050bc1d4 from /health), signed in as the demo
    customer with window.fetch wrapped:
      * "do you sell any laptops?" produced the chat name "Available
        laptops for sale" -- model-written, not the question verbatim;
      * exactly ONE POST .../title was issued for that chat;
      * a SECOND turn in the same chat issued no further title request
        (two bridge turns, one title request) and the name did not change;
      * the five pre-existing chats still show their fallback names.

    NOT VERIFIED LIVE: the failure paths. Taking the agent down to watch
    a chat keep its fallback is not worth an outage; four separate
    failure paths are covered by route tests.
